# Minimal config for embedding extraction
artifacts:
  root: artifacts

preprocess:
  max_edge: 512

features:
  device: cuda   # or cpu
  batch_size: 32
  amp_dtype: bf16  # bf16 or fp16
  l2_normalize: true
